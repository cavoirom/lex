# Lex devlog 11 - 2026-05-30

## Goals

- Compose UTF-16.
- Compose replacement.
- Remove remaining `SeedSpan` in tests, use `Span.init*` instead.

## Zig knowledge

- Zig pointer has:
  - Single item pointer: `*T`, e.g. `*State`, `*[16]u8`.
  - Many-item pointer: `[*]T`, e.g. `[*]u8`.

## Lex knowledge

- The compose replacement only operate within buffer_effective length.

## Code

- Define the code pattern for `Span.to_utf16` then ask the bot to repeat the boilerplate. We still
  own the code shape.
