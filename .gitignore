# Lex app binary.
macos/Lex.app/Contents/MacOS/Lex
# Lex app signature data.
macos/Lex.app/Contents/_CodeSignature
# Zig cache.
.zig-cache
# Build environment cache (vendored Zig, module caches, generated rcfile).
.cache
